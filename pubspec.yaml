name: moradia_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '^3.11.0'

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.17.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Aqui está a configuração da sua logo com o espaçamento correto
  assets:
    - assets/images/logo.jpg
    - assets/map_styles/
